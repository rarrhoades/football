<h1>Listing drafts</h1>

<table>
  <tr>
    <th>Owner</th>
    <th>Player</th>
    <th>Year</th>
    <th>Round</th>
    <th>Pick</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @drafts.each do |draft| %>
  <tr>
    <td><%= draft.owner_id %></td>
    <td><%= draft.player_id %></td>
    <td><%= draft.year %></td>
    <td><%= draft.round %></td>
    <td><%= draft.pick %></td>
    <td><%= link_to 'Show', draft %></td>
    <td><%= link_to 'Edit', edit_draft_path(draft) %></td>
    <td><%= link_to 'Destroy', draft, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Draft', new_draft_path %>
